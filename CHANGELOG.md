# 更新日志

本文件记录 Vibe Canvas 所有值得关注的变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
并遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.9.0] - 2026-08-18

### 新增

- 在三级 Claim 后增加默认折叠的第四级逐字原文证据；点击 Claim 或按 Enter / 空格即可展开、收起。
- `upsert_claim.sourceQuote`：Skill 提交支持 Claim 的最短原文片段，服务端校验其必须是当前 `userText` 的精确子串。
- 浏览器投影最多展示最近 5 条 Claim 证据，并提示从左侧完整原文查看更早内容。

### 变更

- 逐字证据只进入本地 Browser 投影；Codex 的紧凑投影继续只返回证据数量和最近来源 ID，避免长对话 Token 膨胀。
- 脑图展开后动态增加第四列，并为可展开 Claim 增加 `原文 N` 状态与键盘可访问性。
- 测试套件由 32 项扩展为 33 项，并增加折叠、鼠标、键盘和逐字校验的浏览器视觉测试。

### 修复

- 取消某轮 Pick 时，同时移除该轮贡献的 Claim 原文证据；重新 Pick 后可由本地 session 重建。

## [0.8.2] - 2026-08-18

### 新增

- 类型化概念图：区分 Domain、canonical Topic / Question、typed Claim、跨主题 Relation 与来源证据。
- `merge_topics`、`merge_claims`、`move_claim` 和类型化 `link` 操作，用于跨轮合并、主题拆分与交叉关系。
- 40 条最近活跃 Claim 的紧凑匹配索引，以及累计 `overview`。
- 12 轮重复话题 MCP 回放和 100 轮证据边界测试。

### 变更

- 默认投影限制为 25 个可见节点，并优先显示最近仍在讨论的 Domain、Topic 和 Claim。
- 同一 Canvas 内，相同问题与命题通过稳定 ID 聚合并累积多轮证据；交叉观点只存一次。
- 返回给 Codex 的证据改为数量与最近 3 个来源 ID，完整溯源继续保存在本地 session JSON。
- 测试套件由 13 项扩展为 32 项。

### 修复

- 修复后续轮次意外新造 ID 时，规范化后完全相同的问题或观点仍会重复的问题。
- 修复摘要按轮次持续拼接、长对话越用越长的问题。
- 修复早期主题长期占据投影预算、近期讨论无法进入默认视图的问题。
- 修复隐藏 Claim 无法参与后续轮次去重比对的问题。

## [0.7.0] - 2026-08-17

### 新增

- Codex 左侧唯一输入、右侧 Browser 自动投影的完整交互链路。
- 逐字原文、结构化理解与动态思维导图三层视图。
- AI 自动 Pick、候选缓冲、后续复判与人工最终覆盖机制。
- 蓝色默认与暖色备选主题，以及跨画板偏好持久化。
- 本地 MCP/HTTP session 服务、JSON 持久化和 revision 增量刷新。
- 13 条公开接口行为测试与视觉冒烟测试入口。

### 变更

- Pick 控件改为标准圆形 Checkbox：绿色表示已选，灰色表示未选。
- 主题选择改为带“蓝色 / 暖色”标签的二态 Switch。
- 每轮同步只读取紧凑投影并写入当前增量，减少不必要的上下文回放。

### 修复

- 移除画板内的二次输入、发送、保存和整理操作。
- “对话原文”只展示用户逐字输入，不再混入 AI 提炼。
- 隐藏用户无须理解的内部 `rN` revision 标记。
