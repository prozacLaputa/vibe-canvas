{
  "name": "vibe-canvas-codex-plugin",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "visual:smoke": "node tests/visual-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
